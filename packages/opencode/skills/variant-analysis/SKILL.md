---
name: variant-analysis
description: Hunt for functional bug variants across the entire monorepo based on a single localized fix.
---

# Variant Analysis

When you fix a bug in one component, use this skill to write a pattern-match query (similar to Semgrep/CodeQL) to locate and fix all other instances of that exact same anti-pattern across the codebase.
