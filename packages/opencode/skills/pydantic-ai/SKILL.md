---
name: pydantic-ai
description: Use typed, structured data interactions to replace brittle JSON models.
---

# Pydantic AI Integrations

When writing backend code or tool handlers in Python that interact with LLMs, enforce schema validation natively with PydanticAI to prevent hallucinated keys or miscast field types.
